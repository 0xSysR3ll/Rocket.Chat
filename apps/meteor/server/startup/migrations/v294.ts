import { addMigration } from '../../lib/migrations';

addMigration({
	version: 294,
	name: 'Enterprise migration stub - removed in FOSS version',
	async up() {
		// This migration was removed as part of enterprise features
		// Stub implementation for FOSS version
	},
});
